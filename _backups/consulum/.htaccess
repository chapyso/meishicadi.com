<IfModule mod_rewrite.c>
    RewriteEngine On

    # If the requested path does not already point into public, send it there.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>
